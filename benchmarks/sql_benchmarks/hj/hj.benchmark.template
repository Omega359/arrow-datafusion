
name Q${QUERY_NUMBER_PADDED}
group hj

echo Loading hj data (tpch sf${BENCH_SIZE:-1}, parquet)

load sql_benchmarks/hj/init/load.sql

assert I
SELECT count(*) > 0 FROM customer
----
true

expect_plan HashJoinExec

run sql_benchmarks/hj/queries/q${QUERY_NUMBER_PADDED}.sql
