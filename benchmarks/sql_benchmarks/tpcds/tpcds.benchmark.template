name Q${QUERY_NUMBER_PADDED}
group tpcds

echo Loading tpcds sf${BENCH_SIZE:-1} data

load sql_benchmarks/tpcds/init/load.sql

assert I
SELECT COUNT(*) > 0 from web_site;
----
true

run ../datafusion/core/tests/tpc-ds/${QUERY_NUMBER}.sql

result sql_benchmarks/tpcds/results/sf${BENCH_SIZE:-1}/${QUERY_NUMBER}.csv
