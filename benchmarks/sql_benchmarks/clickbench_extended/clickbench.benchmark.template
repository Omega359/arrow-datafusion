name Q${QUERY_NUMBER_PADDED}
group clickbench_extended
subgroup ${CLICKBENCH_TYPE:-single}

echo Loading clickbench ${CLICKBENCH_TYPE:-single} hits table

load sql_benchmarks/clickbench/init/load-${CLICKBENCH_TYPE:-single}.sql

assert I
SELECT COUNT(*) > 0 from hits;
----
true

run sql_benchmarks/clickbench_extended/queries/q${QUERY_NUMBER_PADDED}.sql

result sql_benchmarks/clickbench_extended/results/q${QUERY_NUMBER_PADDED}.csv
