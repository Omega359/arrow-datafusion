name Q${QUERY_NUMBER_PADDED}
group clickbench_sorted

init sql_benchmarks/clickbench/init/set_config.sql

echo Loading clickbench_sorted hits table with sort order: ${SORTED_BY:-EventTime} ${SORTED_ORDER:-ASC}

load sql_benchmarks/clickbench_sorted/init/load.sql

assert I
SELECT COUNT(*) > 0 from hits;
----
true


run sql_benchmarks/clickbench_sorted/queries/q${QUERY_NUMBER_PADDED}.sql

result sql_benchmarks/clickbench_sorted/results/q${QUERY_NUMBER_PADDED}.csv
