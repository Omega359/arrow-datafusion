
name Q${QUERY_NUMBER_PADDED}
group taxi

echo Loading taxi data

load sql_benchmarks/taxi/init/load.sql

assert I
SELECT COUNT(*) > 0 from trips;
----
true

run
${RESULT_QUERY}

result sql_benchmarks/taxi/results/q${QUERY_NUMBER_PADDED}.csv
